remote_theme: pages-themes/cayman@v0.2.0
title: Junior @firedev.se
description: Quick guides and explanations for junior developers
domain: junior.firedev.se       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://junior.firedev.se  # the base hostname and protocol for your site, e.g. http://example.com
markdown: kramdown
plugins:
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-remote-theme